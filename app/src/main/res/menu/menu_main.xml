<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="cz.muni.fi.nofuzzmenu.activity.MainActivity">

    <!-- "Quick Settings" icon for navigating to search settings -->
    <item
            android:id="@+id/action_search_settings"
            android:icon="@drawable/ic_gps_fixed"
            android:title="@string/action_search_settings"
            app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        android:icon="@drawable/ic_settings"
        app:showAsAction="ifRoom" />
</menu>
