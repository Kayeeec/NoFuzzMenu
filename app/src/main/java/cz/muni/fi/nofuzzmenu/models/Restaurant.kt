package cz.muni.fi.nofuzzmenu.models

/**
 * A common class representing a restaurant info parsed from various sources. Contains basic info about the restaurant
 * (address, name) and loaded info about its current menu (if loaded).
 */
class Restaurant {
}